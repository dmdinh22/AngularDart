// this is the card object to be used to render on the page.

class Card {
  final String question;
  final List<String> options;
  final int answer;

  Card(this.question, this.options, this.answer);
}

